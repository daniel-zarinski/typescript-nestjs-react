export * from './routes';

export enum Role {
  User = 'user',
  Admin = 'admin',
  Dev = 'dev',
}

export enum Message {
  Info = 'info',
  Success = 'success',
  Warning = 'warning',
  Error = 'error',
}
